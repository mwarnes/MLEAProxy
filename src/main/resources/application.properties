# ================================================================
# Spring Boot Application Configuration
# ================================================================

# ----------------------------------------------------------------
# Logging Configuration
# ----------------------------------------------------------------
# Set application log level: INFO | WARN | ERROR | TRACE | DEBUG
logging.level.root=INFO
logging.file=mleaproxy.log
logging.pattern.file=%d %p %c{1.} [%t] %m%n
logging.pattern.console=%d{HH:mm:ss.SSS} [%t] %-5level %logger{36} - %msg%n

# ----------------------------------------------------------------
# XML User Repository (Optional)
# ----------------------------------------------------------------
# Path to users.xml file for OAuth and SAML user/role lookup
# Default: classpath:users.xml (bundled in JAR)
# Override via command line: --users-xml=/path/to/users.xml
# Override via environment: USERS_XML_PATH=/path/to/users.xml
#users.xml.path=classpath:users.xml
# Commented out to use default classpath resource loading


