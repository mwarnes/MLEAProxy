# Test configuration for OAuth Token Handler
# This ensures default roles are properly configured during testing

# Default roles for OAuth token generation
oauth.default.roles=user

# Disable JSON user repository for clean testing
users.json.path=

# Disable XML user repository for clean testing 
users.xml.path=

# Test logging configuration
logging.level.com.marklogic.handlers.undertow.OAuthTokenHandler=DEBUG